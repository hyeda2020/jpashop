spring: # 테스트용 In-Memory DB로 설정
# 스프링 부트는 아래처럼 datasource 설정이 없으면 기본적으로 메모리 DB로 세팅함
#  datasource:
#    url: jdbc:h2:tcp://localhost/~/jpashop
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop
#    properties:
#      hibernate:
##        show_sql: true
#        format_sql: true

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.orm.jdbc.bind: trace # 쿼리 파라미터 로그 남기기